package com.syntax.koetter.patternpatrol.adapter

/*
import androidx.fragment.app.Fragment
import androidx.fragment.app.FragmentManager
import androidx.fragment.app.FragmentPagerAdapter

// This code creates a custom PagerAdapter that returns a Fragment for each tab. In this example,
// there are two tabs (Tab1Fragment and Tab2Fragment) and the getPageTitle() method returns
// the title for each tab.
class PagerAdapter(fm: FragmentManager): FragmentPagerAdapter(fm) {

        override fun getItem(position: Int): Fragment {
            return when (position) {
                0 -> Tab1Fragment()
                1 -> Tab2Fragment()
                else -> throw IllegalArgumentException("Invalid position: $position")
            }
        }

        override fun getCount(): Int = 2

        override fun getPageTitle(position: Int): CharSequence? {
            return when (position) {
                0 -> "Tab 1"
                1 -> "Tab 2"
                else -> null
            }
        }
    This code creates a custom PagerAdapter that returns a Fragment for each tab. In this example, there are two tabs (Tab1Fragment and Tab2Fragment) and the getPageTitle() method returns the title for each tab.

    Finally, create the Tab1Fragment and Tab2Fragment classes that extend Fragment and inflate their respective layouts.
    kotlin
    Copy code
    class Tab1Fragment : Fragment() {
        override fun onCreateView(inflater: LayoutInflater, container: ViewGroup?, savedInstanceState: Bundle?): View? {
            return inflater.inflate(R.layout.fragment_tab1, container, false)
        }
    }

    class Tab2Fragment : Fragment() {
        override fun onCreateView




    }
}*/
